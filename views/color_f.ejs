<!DOCTYPE html>
<html lang="ja">
<head><meta charset="UTF-8"><title>色彩フォーム</title></head>
<body>
    <h1>色の<%= mode === 'add' ? '追加' : '編集' %></h1>
    <% const actionUrl = mode === 'add' ? '/color/add' : '/color/update/' + data.id; %>
    <form action="<%= actionUrl %>" method="POST">
        <label>色名: <input type="text" name="name" value="<%= data ? data.name : '' %>" required></label><br>
        <label>コード(#HEX): <input type="text" name="code" value="<%= data ? data.code : '' %>" required></label><br>
        <button type="submit"><%= mode === 'add' ? '登録' : '更新' %></button>
    </form>
    <a href="/color">キャンセル</a>
</body>
</html>